import 'package:flutter/material.dart';
//Criar Scaffold com listview + cards para carregar o projeto

//incluir chamada na API passando o IP + Pasta do Projeto + Public

//http://10.56.44.35/chronos_public/public/api/projetos

class WebViewProjetos extends StatelessWidget {
  const WebViewProjetos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(),
    );
  }
}
